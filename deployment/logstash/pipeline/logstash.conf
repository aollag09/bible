input {
  jdbc {
    jdbc_driver_library => "/usr/share/logstash/logstash-core/lib/jars/mysql-connector-java.jar"
    jdbc_driver_class => "com.mysql.cj.jdbc.Driver"
    jdbc_connection_string => "jdbc:mysql://database:3306/bible?useSSL=false"
    jdbc_user => "bible_user"
	jdbc_password => "docker"
    statement => "select * from bible_version_key"
    # If no schedule, Select query will be executed only once
    schedule => "1 * * * *"
  }
}


## Add your filters / logstash plugins configuration here

output {
	elasticsearch {
		index => "bible"
		hosts => "elasticsearch:9200"
        document_id => "%{id}"
	}
}

